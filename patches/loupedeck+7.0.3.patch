diff --git a/node_modules/loupedeck/connections/serial.js b/node_modules/loupedeck/connections/serial.js
index b7fa147..c7b2cb7 100644
--- a/node_modules/loupedeck/connections/serial.js
+++ b/node_modules/loupedeck/connections/serial.js
@@ -46,9 +46,30 @@ export default class LoupedeckSerialConnection extends EventEmitter {
         return results
     }
     close() {
-        if (!this.connection) return
-        this.send(Buffer.from(WS_CLOSE_FRAME), true)
-        return new Promise(res => this.connection.close(res))
+        if (!this.connection) {
+            return Promise.resolve()
+        }
+
+        return new Promise((resolve, reject) => {
+            this.connection.write(Buffer.from(WS_CLOSE_FRAME), writeErr => {
+                if (writeErr) {
+                    console.error(
+                        'Loupedeck: Failed to write close frame:',
+                        writeErr.message
+                    )
+                }
+
+                this.connection.close(closeErr => {
+                    if (closeErr) {
+                        if (closeErr.message === 'Port is not open') {
+                            return resolve()
+                        }
+                        return reject(closeErr)
+                    }
+                    resolve()
+                })
+            })
+        })
     }
     async connect() {
         this.connection = new SerialPort({ path: this.path, baudRate: 256000 })
